# Contributing

1. Create a focused branch.
2. Install `requirements-dev.txt`.
3. Add tests for any calculation or parser change.
4. Run `python -m pytest` and `python -m ruff check .`.
5. Keep financial claims descriptive and document model limitations.

Do not add course materials, proprietary datasets, copied provider responses,
or real API credentials to pull requests.
